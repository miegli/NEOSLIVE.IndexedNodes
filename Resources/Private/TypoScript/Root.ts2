# Container TS Object
prototype(NEOSLIVE.IndexedNodes:Container) < prototype(TYPO3.Neos:Content) {

	nodes = ${NEOSLIVE.IndexedNodes.get(node)}

	class = ''

	itemRendering = TYPO3.Neos:ContentCase

	@cache {
		mode = 'uncached'
		context {
			1 = 'node'
			2 = 'documentNode'
		}
	}

}
